use Suraho_UNIVER
CREATE TABLE RESULTS(
	ID int IDENTITY(1, 1) PRIMARY KEY NOT NULL,
	STUDENT_NAME nvarchar(50) NOT NULL,
	DATA_BASE int NOT NULL,
	PROGRAMMING_LANGUAGES int NOT NULL,
	AVER_VALUE as (DATA_BASE + PROGRAMMING_LANGUAGES) / 2
);

INSERT INTO RESULTS(STUDENT_NAME, DATA_BASE, PROGRAMMING_LANGUAGES)
	VALUES ('Дмитрий', 8, 10), ('Иван', 10, 6), ('Артем', 6, 4);
	
SELECT * FROM RESULTS;

DROP TABLE RESULTS;